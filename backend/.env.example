PORT=5000
MONGO_URI=mongodb://localhost:27017/event-volunteer-portal

# Optional — enables Claude-powered summaries for Analytics & Trust Cards.
# Without this key, the app uses template-based fallback text (still works).
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022
